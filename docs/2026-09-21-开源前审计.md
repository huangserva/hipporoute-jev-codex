# 开源前密钥与隐私审计

一句话结论：当前工作区可以在完成本文所列检查后公开；未发现 TypeSafe
key、caller secret、Bearer/JWT 或 ChatGPT 登录态，但可达 Git 历史仍保留
作者身份元数据、旧 home 绝对路径、实验配置哈希和曾误跟踪的 `pyc` 路径，
如果仓库公开时要求匿名或不暴露这些历史信息，应先重写历史。

## 审计边界与方法

审计覆盖当前工作区、所有 Git 跟踪文件、所有 refs 的完整 `git log -p`、
提交 author/committer metadata、忽略文件规则，以及真实协议 fixture。主要扫描：

- `API-key-` 前缀和当前本机 TypeSafe key 的精确字节串；
- 当前 caller-secret 的精确字节串；
- `Authorization: Bearer`、JWT、access/refresh token 和 ChatGPT account 字段；
- 邮箱、作者用户名、home 绝对路径、主机名、UUID/线程 ID 和长哈希；
- `runtime/`、JSONL、线程状态、本地配置、服务配置和备份是否被跟踪。

两个真实 secret 只用于内存中的精确匹配，命令输出只记录命中数，secret
本身没有被打印、复制或写入仓库。

## 发现

| 范围 | 结果 | 处理 |
|---|---|---|
| 当前 TypeSafe key / caller secret | 整个工作区（含 ignored runtime）和全部可达提交均为 0 个精确命中 | 无需因这两个 secret 泄露而轮换 |
| Bearer、JWT、ChatGPT token | 当前文件和全部可达提交均为 0 命中 | 无 |
| 当前跟踪文件 | 无个人 home 路径、真实邮箱、主机名、真实 UUID/线程 ID或实验配置哈希 | 已通用化 |
| Git metadata | 初审发现私人邮箱；历史重写后仅保留 noreply 身份 | 已完成 mailmap 重写 |
| 历史内容 | 初审发现旧 home 路径和历史 `pyc` blob | 已从全部可达历史替换或删除 |
| 历史报告 | 初审发现 4 个实验配置、fixture 或 capture SHA-256 | 已从全部可达历史替换为占位符 |
| 忽略目录 | 本地 `runtime/` 中确有请求日志、线程 ID 和 prompt-derived 数据；下列 3 个历史 capture 含真实 Bearer/JWT-like 登录态 | 保持忽略，未加入 Git；不得打包上传 runtime |

工作区中认证命中的具体位置（只报告文件，不打印值）：

- `runtime/phase2-capture/requests.jsonl`
- `runtime/phase2-capture/requests-response-probe.jsonl`
- `runtime/review-a1/requests.jsonl`

此外，`runtime/bench/t1-calibrate-high-20260920/sessions/001-80k-live_all-r1-a1.jsonl`
含与 Git author metadata 相同的私人邮箱；另一个 benchmark session 含普通
email-like 文本但不等于作者邮箱。它们同样只存在于 ignored runtime。

这些文件从未被 Git 跟踪，`.gitignore` 的 `runtime/` 会阻止常规 `git add`；
但发布源码压缩包或使用 `git add -f` 时仍必须明确排除整个目录。

配置 SHA-256 不是认证 secret，但它是作者机器的稳定指纹，因此也从公开
HEAD 的报告中删除。提交对象自己的 Git object ID 不属于这项清理范围。

## 本轮修改

- `.gitignore` 明确覆盖 `runtime/`、`*.jsonl`、`threads.json`、
  `config.local.toml`、`config.service.toml`、`config.e2e.toml`、常见备份；
- 将运行中的 `config.service.toml` 从 Git 索引移除但保留磁盘文件，新增无
  凭据的 `config.service.example.toml`；
- 将历史实验报告移到 `docs/experiments/`，增加单机小样本免责声明，并将
  home 路径、用户名、备份时间戳和哈希改为相对路径或占位符；
- 安装/启停/watchdog 脚本不再写死作者目录或代理端口；代理通过环境变量；
- Jev key 标准化为环境变量、权限 600 的 `~/.jev.env`、或 TOML 的
  `jev.key_file` 三种外部来源；配置中的 literal key 字段会被拒绝；
- fixture `tests/fixtures/codex-0.155.1-compaction-request.json` 再次检查：
  仅有 `thread-redacted`/`turn-redacted` 等合成值，无认证 header、邮箱、
  home 路径、UUID、encrypted content 或用户内容；
- `shadow-report.py` 增加互斥的 `--since`、`--hours` 和既有 `--days`，时间
  窗口在会话与费用聚合前应用。

`shadow-report.py --hours 3` 已对当前 shadow 日志成功运行：读取 15 条、5
个根会话，无畸形行；输出只有聚合数据，没有线程 ID 或 task 文本。

## 是否需要重写历史

没有发现 secret，所以不存在因凭据泄露而必须紧急重写历史的理由。初审时
仍需由仓库所有者决定是否为了隐藏旧 home 路径、私人 Git 邮箱和实验机器
指纹而重写；所有者随后明确授权，执行结果记录在本文末尾。

建议先建立镜像备份，然后在仓库外准备 replacement 和 mailmap 文件：

```bash
git clone --mirror <repository-url> codex-jev-router-backup.git

# /tmp/replacements.txt 中填写真实旧值；不要提交该文件。
printf '%s\n' \
  'literal:<old-home-path>==><home>' \
  'literal:<old-config-sha256>==><redacted-sha256>' \
  > /tmp/replacements.txt

# /tmp/mailmap 中填写旧身份和希望公开的新身份；不要提交该文件。
printf '%s\n' \
  'Public Name <public@example.invalid> Private Name <private-email>' \
  > /tmp/mailmap

git filter-repo --force \
  --replace-text /tmp/replacements.txt \
  --mailmap /tmp/mailmap \
  --path-glob '*/__pycache__/*' \
  --path-glob '*.pyc' \
  --invert-paths

# 重新做本报告中的全历史扫描，再由所有者确认后推送。
git push --force --all
git push --force --tags
```

最终已选择重写；旧路径和哈希只保留在本地镜像备份中，不存在于待公开仓库
的可达对象。

## 发布门禁

发布前最终门禁结果：`python3 -m unittest -q` 共 **143 项全部通过**；
`python3 -m compileall -q codex_jev_router bench tests scripts` 退出码为 0。
当前 HEAD 密钥扫描、完整历史扫描、fixture 扫描和 ignore 验证结果如上。

## 历史重写执行记录

执行日期：2026-09-21。重写前先确认 4319 的 codex-jev-router 与 4202 的
Codex Router 健康，然后建立镜像备份：

```text
../codex-jev-router-backup-20260921.git
```

该备份包含重写前的全部旧历史与隐私信息，**不得上传或共享**。备份和原仓库
重写前均为 69 个提交，原始 HEAD 一致。

`git-filter-repo` 使用下载到 `/tmp` 的官方单文件脚本和系统 Python 执行。
核心命令为：

```bash
/usr/bin/python3 /tmp/git-filter-repo --force \
  --path-glob '*/__pycache__/*' --path-glob '*.pyc' --invert-paths

/usr/bin/python3 /tmp/git-filter-repo --force \
  --replace-text /tmp/codex-jev-router-replacements.txt \
  --mailmap /tmp/codex-jev-router-mailmap.txt
```

第一次删除全部历史 `__pycache__` 与 `*.pyc`，第二次替换作者 home 路径、4
个历史 SHA-256 并将 author/committer 邮箱映射到 GitHub noreply。严格字面
门禁还发现源码中的 Bearer-scheme 与审计文档中的 API-key 前缀说明会造成
无害的 grep 命中，因此又做了一次等价文本重写：源码改为字符串拼接，不改变
Authorization header 的运行值。

重写后、追加本记录前仍为 69 个提交，没有因删除 pycache 产生空提交；提交
本记录后为 70 个提交。重写后的全历史复验：

| 检查项 | 命中数 |
|---|---:|
| 旧作者 home/用户名路径串 | 0 |
| 旧私人 Gmail 身份 | 0 |
| TypeSafe key 前缀字面串 | 0 |
| `Bearer` 加空格字面串 | 0 |
| 4 个被替换的 SHA-256（逐个检查） | 0 / 0 / 0 / 0 |
| 当前跟踪 `pyc` / 全历史 `pyc` | 0 / 0 |

`git log --format='%an <%ae>' | sort -u` 只输出
`huangserva <huangserva@users.noreply.github.com>`。`python3 -m unittest
discover -s tests` 为 143 项全部通过，compileall 通过；重写后两个健康端点
再次返回正常。替换规则和 mailmap 只存在于 `/tmp`，未进入仓库。
